describe("is.net.tld", function()
	local t, is
	setup(function()
    t = require "t"
    is = t.is
	end)
  it("meta", function()
    assert.truthy(is)
    assert.truthy(is.callable(is.net.tld))
  end)
  it("positive", function()
    assert.is_true(is.net.tld('COM'))
    assert.is_true(is.net.tld('CoM'))
    assert.is_true(is.net.tld('com'))
    assert.is_true(is.net.tld('.com'))
    assert.is_true(is.net.tld('.рф'))
    assert.is_true(is.net.tld('.info'))
    assert.is_true(is.net.tld('com.ru'))
    assert.is_true(is.net.tld('.com.ru'))
  end)
  it("negative", function()
    assert.is_nil(is.net.tld('8.8.8.8'))
    assert.is_nil(is.net.tld(''))
    assert.is_nil(is.net.tld('.'))
    assert.is_nil(is.net.tld('.local'))
    assert.is_nil(is.net.tld('local'))
    assert.is_nil(is.net.tld({}))
    assert.is_nil(is.net.tld({'type'}))
    assert.is_nil(is.net.tld(0))
    assert.is_nil(is.net.tld(''))
    assert.is_nil(is.net.tld(false))
    assert.is_nil(is.net.tld(true))
  end)
  it("nil", function()
    assert.is_nil(is.net.tld(nil))
    assert.is_nil(is.net.tld())
  end)
end)